# ADR-2114: UnitOfWork Integration

**Status:** Proposed  
**Date:** 2026-07-20  
**Deciders:** Architecture Team  

---

## Context

El AI Core necesita acceder a los repositorios del dominio. El UnitOfWork existente en la API es la forma recomendada de acceder a los datos con transacciones.

Necesitamos:
1. Crear un factory que el AI Core pueda usar
2. Mantener compatibilidad con el UnitOfWork existente
3. Permitir testing con mocks

---

## Decision

Crear un `AIUnitOfWorkFactory` que exponga el UnitOfWork de la API al AI Core.

### Implementación

```python
# core/ai/integration/uow_factory.py
from __future__ import annotations

from contextlib import asynccontextmanager
from typing import TYPE_CHECKING, AsyncGenerator

if TYPE_CHECKING:
    from apps.api.app.infrastructure.unit_of_work import UnitOfWork


class AIUnitOfWorkFactory:
    """
    Factory para crear UnitOfWork desde el AI Core.
    
    Este factory expone el UnitOfWork de la API de forma
    que el AI Core pueda usar sin耦合 directo.
    
    Usage:
        # En DI setup
        factory = AIUnitOfWorkFactory()
        
        # En gateway
        async with factory() as uow:
            devices = await uow.devices.list_by_tenant(...)
    """
    
    def __init__(self, session_factory=None):
        """
        Args:
            session_factory: Función que crea AsyncSession.
                             Si es None, usa la default de la app.
        """
        self._session_factory = session_factory
    
    @asynccontextmanager
    async def __call__(
        self,
    ) -> AsyncGenerator[AIUnitOfWork, None]:
        """
        Crea un UnitOfWork para usar en contexto async.
        
        Usage:
            async with factory() as uow:
                devices = await uow.devices.list_by_tenant(...)
                await uow.commit()
        """
        from apps.api.app.infrastructure.unit_of_work import UnitOfWork
        from sqlalchemy.ext.asyncio import AsyncSession
        
        if self._session_factory:
            session = self._session_factory()
        else:
            from apps.api.app.core.database import get_session_factory
            factory = get_session_factory()
            session = factory()
        
        try:
            uow = AIUnitOfWork(session)
            yield uow
            await uow.commit()
        except Exception:
            await session.rollback()
            raise
        finally:
            await session.close()


class AIUnitOfWork:
    """
    Wrapper alrededor de UnitOfWork que expone solo
    lo que el AI Core necesita.
    
    Provee acceso tipado a los repositorios.
    """
    
    def __init__(self, session: AsyncSession):
        self._session = session
        self._committed = False
        
        # Lazy loading de repositorios
        self._devices: "DeviceRepositoryImpl | None" = None
        self._incidents: "IncidentRepositoryImpl | None" = None
        self._knowledge: "KnowledgeRepositoryImpl | None" = None
        self._recommendations: "RecommendationRepositoryImpl | None" = None
    
    @property
    def devices(self) -> "DeviceRepositoryImpl":
        """Acceso al repositorio de dispositivos."""
        if self._devices is None:
            from apps.api.app.domain.device.repository import DeviceRepositoryImpl
            self._devices = DeviceRepositoryImpl(self._session)
        return self._devices
    
    @property
    def incidents(self) -> "IncidentRepositoryImpl":
        """Acceso al repositorio de incidentes."""
        if self._incidents is None:
            from apps.api.app.domain.incident.repository import IncidentRepositoryImpl
            self._incidents = IncidentRepositoryImpl(self._session)
        return self._incidents
    
    @property
    def knowledge(self) -> "KnowledgeRepositoryImpl":
        """Acceso al repositorio de conocimiento."""
        if self._knowledge is None:
            from apps.api.app.domain.knowledge.repository import KnowledgeRepositoryImpl
            self._knowledge = KnowledgeRepositoryImpl(self._session)
        return self._knowledge
    
    @property
    def recommendations(self) -> "RecommendationRepositoryImpl":
        """Acceso al repositorio de recomendaciones."""
        if self._recommendations is None:
            from apps.api.app.domain.recommendation.repository import RecommendationRepositoryImpl
            self._recommendations = RecommendationRepositoryImpl(self._session)
        return self._recommendations
    
    async def commit(self) -> None:
        """Confirma los cambios."""
        await self._session.commit()
        self._committed = True
    
    async def rollback(self) -> None:
        """Revierte los cambios."""
        await self._session.rollback()
        self._committed = False


# Instancia global para la aplicación
_global_factory: AIUnitOfWorkFactory | None = None


def get_uow_factory() -> AIUnitOfWorkFactory:
    """Obtiene la instancia global del factory."""
    global _global_factory
    if _global_factory is None:
        _global_factory = AIUnitOfWorkFactory()
    return _global_factory


def set_uow_factory(factory: AIUnitOfWorkFactory) -> None:
    """Establece la instancia global del factory."""
    global _global_factory
    _global_factory = factory
```

### Integración con DomainGatewayAdapter

```python
# core/ai/integration/domain_adapter.py
from core.PHASE_2.ai.integration.uow_factory import get_uow_factory

class DomainGatewayAdapter:
    """Actualizado para usar factory global."""
    
    def __init__(
        self,
        uow_factory: AIUnitOfWorkFactory | None = None,
    ):
        self._uow_factory = uow_factory or get_uow_factory()
    
    # ... resto de la implementación igual
```

### Testing con Mock

```python
# tests/ai_core/test_gateways.py
import pytest
from unittest.mock import AsyncMock, MagicMock

from core.PHASE_2.ai.integration.uow_factory import AIUnitOfWorkFactory, AIUnitOfWork
from core.PHASE_2.ai.domain import DeviceGateway

@pytest.fixture
def mock_uow_factory():
    """Factory que retorna un UOW mockeado."""
    mock_uow = MagicMock(spec=AIUnitOfWork)
    mock_uow.devices = MagicMock()
    mock_uow.devices.get_by_id = AsyncMock(return_value=None)
    mock_uow.devices.list_by_tenant = AsyncMock(return_value=([], 0))
    
    async def mock_context():
        yield mock_uow
    
    factory = AIUnitOfWorkFactory()
    factory._session_factory = lambda: MagicMock()
    
    return factory, mock_uow

@pytest.mark.asyncio
async def test_device_gateway_with_mock(mock_uow_factory):
    """Test de gateway con UOW mockeado."""
    factory, mock_uow = mock_uow_factory
    
    adapter = DomainGatewayAdapter(factory)
    gateway = adapter.create_device_gateway()
    
    # El gateway usa el factory para crear UOW
    result = await gateway.get_by_id("dev-001", "tenant-001")
    
    # Verificar llamadas
    assert mock_uow.devices.get_by_id.called
```

---

## Consequences

### Positive
- Acceso directo a repositorios reales
- Transacciones consistentes
- Fácil de testear con mocks
- No rompe encapsulamiento existente

### Negative
- Dependencia directa de estructura de API
- Session management externalizado

---

## References

- [EPIC 10 README](../../epics/epic10-domain-bridge/README.md)
- UnitOfWork implementation: `apps/api/app/infrastructure/unit_of_work.py`
- Domain Gateway Adapter: ADR-2110
