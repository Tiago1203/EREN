# ADR-2111: Memory Bridge Pattern

**Status:** Proposed  
**Date:** 2026-07-20  
**Deciders:** Architecture Team  

---

## Context

El MemoryManager actual solo almacena texto. Necesitamos:
1. Almacenar referencias a entidades del dominio
2. Mantener mapeo de IDs a tipos de entidad
3. Poder recuperar entidades relacionadas desde la memoria

---

## Decision

Crear un **MemoryBridge** que extienda el MemoryManager para soportar referencias a dominio.

### Estructura

```python
# core/ai/integration/memory_bridge.py
from __future__ import annotations
from dataclasses import dataclass, field
from datetime import datetime
from enum import Enum
from typing import TYPE_CHECKING

if TYPE_CHECKING:
    from core.PHASE_2.ai.memory.manager import MemoryManager


class EntityType(str, Enum):
    """Tipos de entidades del dominio que pueden ser referenciadas."""
    DEVICE = "device"
    INCIDENT = "incident"
    KNOWLEDGE = "knowledge"
    RECOMMENDATION = "recommendation"
    HOSPITAL = "hospital"
    DEPARTMENT = "department"
    WORK_ORDER = "work_order"
    STAFF = "staff"


@dataclass(frozen=True)
class DomainReference:
    """
    Referencia a una entidad del dominio.
    
    Permite que la memoria referencie entidades reales
    sin copiar todos sus datos.
    """
    entity_type: EntityType
    entity_id: str
    display_name: str
    metadata: dict = field(default_factory=dict)
    
    def to_string(self) -> str:
        """Convierte a representación string para almacenamiento."""
        import json
        return json.dumps({
            "type": self.entity_type.value,
            "id": self.entity_id,
            "name": self.display_name,
            "metadata": self.metadata,
        })
    
    @classmethod
    def from_string(cls, data: str) -> DomainReference:
        """Crea desde string."""
        import json
        parsed = json.loads(data)
        return cls(
            entity_type=EntityType(parsed["type"]),
            entity_id=parsed["id"],
            display_name=parsed["name"],
            metadata=parsed.get("metadata", {}),
        )


@dataclass
class MemoryWithReferences:
    """Memoria con sus referencias a dominio."""
    memory_id: str
    content: str
    references: list[DomainReference]
    tenant_id: str | None
    user_id: str | None


class MemoryBridge:
    """
    Bridge entre MemoryManager y Domain.
    
    Permite almacenar y recuperar referencias a entidades
    del dominio asociadas a memorias.
    
    Usage:
        bridge = MemoryBridge(memory_manager, domain_adapter)
        
        # Almacenar con referencia
        memory_id = bridge.store_with_reference(
            content="Device needs maintenance",
            reference=DomainReference(
                entity_type=EntityType.DEVICE,
                entity_id="dev-001",
                display_name="Ventilator ICU-1",
            ),
            tenant_id="tenant-001",
        )
        
        # Recuperar con referencias resueltas
        memory = bridge.get_with_references(memory_id)
        device = memory.resolved_references[0].entity  # DeviceDTO
    """
    
    def __init__(
        self,
        memory_manager: MemoryManager,
        domain_adapter: DomainGatewayAdapter,
    ):
        self._memory = memory_manager
        self._adapter = domain_adapter
    
    def store_with_reference(
        self,
        content: str,
        reference: DomainReference,
        tenant_id: str | None = None,
        user_id: str | None = None,
        memory_type: MemoryType = MemoryType.SEMANTIC,
        importance: MemoryImportance = MemoryImportance.MEDIUM,
        tags: list[str] | None = None,
    ) -> str:
        """
        Almacena una memoria con referencia a dominio.
        
        Args:
            content: Contenido textual de la memoria
            reference: Referencia a entidad del dominio
            tenant_id: ID del tenant
            user_id: ID del usuario
            memory_type: Tipo de memoria
            importance: Importancia
            
        Returns:
            ID de la memoria creada
        """
        # Combinar contenido con referencia serializada
        full_content = f"{content}\n\n[Reference: {reference.to_string()}]"
        
        # Crear tags con referencia
        reference_tags = tags or []
        reference_tags.append(f"ref:{reference.entity_type.value}")
        reference_tags.append(f"id:{reference.entity_id}")
        
        # Almacenar en memoria
        item = self._memory.store(
            content=full_content,
            memory_type=memory_type,
            tenant_id=tenant_id,
            user_id=user_id,
            importance=importance,
            tags=reference_tags,
            metadata={
                "has_domain_reference": True,
                "entity_type": reference.entity_type.value,
                "entity_id": reference.entity_id,
            },
        )
        
        return item.id
    
    def store_with_references(
        self,
        content: str,
        references: list[DomainReference],
        **kwargs,
    ) -> str:
        """Almacena memoria con múltiples referencias."""
        # Crear contenido combinado
        ref_strings = [ref.to_string() for ref in references]
        full_content = f"{content}\n\n[References: {'; '.join(ref_strings)}]"
        
        # Extraer todos los tags
        all_tags = kwargs.get("tags", [])
        for ref in references:
            all_tags.append(f"ref:{ref.entity_type.value}")
        
        return self._memory.store(
            content=full_content,
            tags=all_tags,
            **kwargs,
        ).id
    
    def get_with_references(
        self,
        memory_id: str,
        resolve: bool = True,
    ) -> MemoryWithReferences | None:
        """
        Obtiene memoria con sus referencias.
        
        Args:
            memory_id: ID de la memoria
            resolve: Si True, resuelve las referencias a entidades reales
            
        Returns:
            Memoria con referencias o None
        """
        item = self._memory.retrieve(memory_id)
        if not item:
            return None
        
        # Extraer referencias del contenido
        references = self._extract_references(item.content)
        
        if resolve and references:
            references = self._resolve_references(references)
        
        return MemoryWithReferences(
            memory_id=memory_id,
            content=item.content,
            references=references,
            tenant_id=item.tenant_id,
            user_id=item.user_id,
        )
    
    def search_by_reference(
        self,
        entity_type: EntityType,
        entity_id: str | None = None,
        tenant_id: str | None = None,
    ) -> list[MemoryWithReferences]:
        """
        Busca memorias por referencia de entidad.
        
        Args:
            entity_type: Tipo de entidad a buscar
            entity_id: ID específico (opcional)
            tenant_id: Filtrar por tenant
            
        Returns:
            Lista de memorias que referencian la entidad
        """
        # Buscar por tags
        tags = [f"ref:{entity_type.value}"]
        if entity_id:
            tags.append(f"id:{entity_id}")
        
        result = self._memory.search(
            tags=tags,
            tenant_id=tenant_id,
        )
        
        memories = []
        for item in result.items:
            references = self._extract_references(item.content)
            memories.append(MemoryWithReferences(
                memory_id=item.id,
                content=item.content,
                references=references,
                tenant_id=item.tenant_id,
                user_id=item.user_id,
            ))
        
        return memories
    
    def _extract_references(self, content: str) -> list[DomainReference]:
        """Extrae referencias del contenido."""
        import json
        import re
        
        references = []
        
        # Buscar patrones [Reference: {...}] o [References: {...}]
        patterns = [
            r'\[Reference: ([^\]]+)\]',
            r'\[References: ([^\]]+)\]',
        ]
        
        for pattern in patterns:
            matches = re.findall(pattern, content)
            for match in matches:
                try:
                    # Intentar parsear como JSON
                    data = json.loads(match)
                    references.append(DomainReference(
                        entity_type=EntityType(data["type"]),
                        entity_id=data["id"],
                        display_name=data["name"],
                        metadata=data.get("metadata", {}),
                    ))
                except (json.JSONDecodeError, KeyError):
                    continue
        
        return references
    
    def _resolve_references(
        self,
        references: list[DomainReference],
    ) -> list[DomainReference]:
        """Resuelve referencias a entidades reales."""
        resolved = []
        
        for ref in references:
            resolved_ref = self._resolve_reference(ref)
            if resolved_ref:
                resolved.append(resolved_ref)
        
        return resolved
    
    def _resolve_reference(self, ref: DomainReference) -> DomainReference | None:
        """Resuelve una referencia individual."""
        # Esta implementación básica solo marca como resuelta
        # La versión completa usaría el domain_adapter
        return DomainReference(
            entity_type=ref.entity_type,
            entity_id=ref.entity_id,
            display_name=ref.display_name,
            metadata={
                **ref.metadata,
                "resolved": True,
            },
        )
```

---

## Consequences

### Positive
- Memorias pueden referenciar entidades reales
- Fácil recuperación de entidades relacionadas
- Búsqueda por referencia de dominio
- Tags incluyen tipo de entidad

### Negative
- Contenido duplicado en algunos casos
- Necesita sincronización si entidad se elimina

---

## References

- [EPIC 10 README](../../epics/epic10-domain-bridge/README.md)
- Memory Manager: `core/ai/memory/manager.py`
