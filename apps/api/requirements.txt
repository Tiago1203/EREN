# EREN API — FastAPI service dependencies.
# Pinned loosely for scaffolding; lock exact versions before first release.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
