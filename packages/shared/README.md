# @eren/shared

Cross-cutting **utilities, types and constants** shared across EREN apps,
core engines and other packages.

> **Status:** placeholder. `src/index.ts` is an empty entry point; no
> functionality is implemented yet.

Put here only code that is genuinely reused and has no better home (generic
helpers, shared enums/types, constants). Domain logic belongs in `core/`.
