# @eren/sdk

Typed **client SDK** for programmatic access to EREN. Wraps the `apps/api`
surface so external consumers and internal apps talk to EREN through one
stable, typed client.

> **Status:** placeholder. `src/index.ts` is an empty entry point; no
> functionality is implemented yet.
