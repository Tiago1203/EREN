# @eren/schemas

Canonical **data contracts and validation schemas** shared across EREN. A single
source of truth for the shapes exchanged between apps, core engines and the SDK.

> **Status:** placeholder. `src/index.ts` is an empty entry point; no schemas or
> functionality are implemented yet.
