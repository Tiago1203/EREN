# @eren/prompts

Versioned **prompt library** for EREN's cognitive engines. Centralizes prompt
templates so they can be reviewed, versioned and reused across `core/*`.

> **Status:** placeholder. `src/index.ts` is an empty entry point; no prompts or
> functionality are implemented yet.
